spring:
  application:
    name: microservices-study

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/microservice_service?useTimezone=true&serverTimezone=UTC
    username: postgres
    password: Lipe@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8082